[Unit]
Description=Picnic mirror worker (drains queue against Google Photos web) — run manually, not a daemon
After=network.target

[Service]
Type=oneshot
WorkingDirectory=%h/coding/picnic/server
ExecStart=/usr/bin/node %h/coding/picnic/server/worker.mjs --cap 50
